import 'package:flutter/material.dart';

class CustomCatalog extends StatelessWidget {
  const CustomCatalog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
